#include <stdio.h>

/**
 * main - entry point
 * @argc: parameter
 * @argv: array of listed argument
 * Return: Always 0 (Success0
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", *argv);

	return (0);
}
